import { AgentOptions, MobileProviderOptions, TransferParams, TransferTokenParams } from './interfaces';
import { HttpAgent } from '@dfinity/agent';
declare class MobileProvider {
    private readonly walletConnectProjectId;
    private readonly window;
    private readonly debug;
    private readonly localIdentity;
    private provider;
    private isWallectConnectPaired;
    private walletConnectSession;
    private delegatedIdentity;
    private isFocused;
    private wcPairUri;
    private connectionPromise;
    constructor(options: MobileProviderOptions);
    initialize(): Promise<void>;
    static isMobileBrowser(): boolean;
    private debugLog;
    pair(): Promise<boolean>;
    createAgent({ host, targets }: AgentOptions): Promise<HttpAgent>;
    isPaired(): boolean;
    disconnect(): boolean;
    requestTransfer(params: TransferParams): Promise<unknown> | undefined;
    requestTransferToken(params: TransferTokenParams): Promise<unknown> | undefined;
    private callViaWalletConnect;
    private createIdentityFromDelegation;
    private createDelegatedIdentity;
}
export default MobileProvider;
